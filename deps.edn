{:paths ["src"]
 :deps {
        ;; kotoba.test (clojure.test-compatible surface), not clojure.test --
        ;; scripts/migrate-clojure-test-to-kotoba-test.cljk. Explicit floor.
        io.github.kotoba-lang/test {:git/url "https://github.com/kotoba-lang/test.git"
                                    :git/sha "2ab4885f99fd25552d380d0f6b0b54c5a72eb7ae"}
        ;; kotoba.lang.text, not clojure.string. Pinned as an explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, so without a floor here
        ;; this repo silently rides whatever a sibling happens to name.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
        ;; kotoba-lang/spec, not malli: this repository's last third-party JVM
        ;; dependency was carried for one `validate` call in an eleven-line
        ;; namespace, and a Maven jar cannot reach the kotoba-WASM host these
        ;; `.cljc` sources claim to run on.
        io.github.kotoba-lang/spec
        {:git/url "https://github.com/kotoba-lang/spec.git"
         :git/sha "ffaa34e69834fe83dabf4c04077339a791585fa2"}}
 :aliases
 {:test {:extra-paths ["test"]
        :extra-deps {io.github.cognitect-labs/test-runner
                     {:git/tag "v0.5.1" :git/sha "dfb30dd6605cb6c0efc275e1df1736f6e90d4d73"}
                     ;; TEST-ONLY. The Kotoba compiler and the KIR
                     ;; interpreter drive the `motion.kotoba` port through
                     ;; `motion-parity-test`, which checks it against the
                     ;; spec-based reference in `codec-primitives.motion`.
                     ;; Neither is linked into anything this repo ships.
                     io.github.kotoba-lang/kotoba-kir
                     {:git/url "https://github.com/kotoba-lang/kotoba-kir.git"
                      :git/sha "136a62e0ad93585c5ecfa6755c82aa4ac2637e14"}
                     io.github.kotoba-lang/amu
                     {:git/url "https://github.com/kotoba-lang/amu.git"
                      :git/sha "6889fa736750348863d2b98d69e688131a2ca515"}}
         :main-opts ["-m" "cognitect.test-runner"]}
  ;; clj-kondo as a Clojars lib (no Homebrew/system install needed).
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts ["-m" "clj-kondo.main" "--lint" "src" "test"
                     "--fail-level" "error"]}}}
